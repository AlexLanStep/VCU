<Window x:Class="LabCarDeskTop.Views.LabCarDT"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525" >
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="auto"/>
      </Grid.ColumnDefinitions>
    </Grid>
    <Label Grid.Column="0" Content="{Binding LabelCarLab}" FontSize="15" HorizontalAlignment="Center"/>
    <Label Grid.Column="1" Content="Time" HorizontalAlignment="Right" Margin="0,0,10,0"/>
    <Grid Grid.Row="1">
      <ContentControl prism:RegionManager.RegionName="ContentRegion" />
    </Grid>
  </Grid>
</Window>
